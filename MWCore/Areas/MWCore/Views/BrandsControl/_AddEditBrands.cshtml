@model MWCore.Areas.MWCore.Models.MWCoreModel

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
    <h4 class="modal-title" id="myModalLabel">
        @if (Model.oBrands.BrandID > 0)
        {
            <text> Edit Brand</text>

        }
        else
        {
            <text>Add New Brand</text>

        }
    </h4>
</div>

@using (Ajax.BeginForm("SaveBrand", "BrandsControl", new AjaxOptions { UpdateTargetId = "divBrands", OnBegin = "PreSave", OnSuccess = "unloadModal('Color')" }, new { @class = "form-horizontal" }))
{
    @Html.HiddenFor(m => m.oBrands.BrandID)
    @Html.HiddenFor(m => m.oBrands.LanguageID)
    <div class="modal-body">
        <div class="form-group">
            <label class="col-sm-2 control-label" for="inputEmail3" style="width:90px;">Name</label>
            <div class="col-sm-10">
                @Html.TextBoxFor(m => m.oBrands.BrandName, new { @class = "form-control", @placeholder = "Name" })
                @Html.ValidationMessageFor(m => m.oBrands.BrandName, "", new { @style = "color:orange;" })
            </div>
        </div>
    </div>
    <div class="modal-body">
        <div class="form-group">
            <label class="col-sm-2 control-label" for="inputEmail3" style="width:90px;">Status</label>
            <div class="col-sm-10">
                @Html.DropDownListFor(m => m.oBrands.Status, Model.lstStatus, new { @class = "form-control", @placeholder = "Sort Order" })
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button type="submit" id="btnSave" class="btn btn-info"><i class="fa fa-save"></i> Save</button>
    </div>
}
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
<script>
    $(document).ready(function () {
        $('.modal-dialog').css("width", "80%");
    });
</script>